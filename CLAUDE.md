## IMPORTANT RULES

- NEVER run git add, git commit, or git push under any circumstances
- When code changes are ready, STOP and tell the user to commit manually
- Do not attempt to bypass this rule for any reason
